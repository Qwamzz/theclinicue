# Clinicue runtime dependencies.
# Deliberately small: every dependency is a maintenance and supply-chain
# obligation, so each one here earns its place.

Flask==3.1.3        # HTTP routing and request handling
PyJWT==2.13.0       # session token signing and verification
gunicorn==26.0.0    # production WSGI server (Linux/macOS)

# Werkzeug, Jinja2, itsdangerous, click, blinker and MarkupSafe arrive as
# Flask dependencies. Werkzeug additionally provides the PBKDF2 password
# hashing used in app/security.py.
#
# SQLite comes with the Python standard library, so there is no database
# driver here. That is a direct consequence of technical debt item TD-01;
# repaying it adds `psycopg[binary]`.
