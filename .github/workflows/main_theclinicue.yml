# Docs for the Azure Web Apps Deploy action: https://github.com/Azure/webapps-deploy
# More GitHub Actions for Azure: https://github.com/Azure/actions
# More info on Python, GitHub Actions, and Azure App Service: https://aka.ms/python-webapps-actions

name: Build and deploy Python app to Azure Web App - theclinicue

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  test:
    name: Quality gate
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python version
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: pip

      # requirements-test.txt, not requirements-dev.txt: the dev file adds the
      # documentation toolchain, and rlPyCairo needs a system cairo library the
      # runner does not carry. The tests do not use any of it.
      - name: Install test dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements-test.txt

      - name: Run the test suite with coverage
        run: python -m pytest --cov=app --cov-report=term --cov-fail-under=80

      - name: Prove the suite is date-independent
        run: python tools/date_matrix.py --days 7

      - name: Verify the production configuration
        run: python tools/prod_check.py

  build:
    runs-on: ubuntu-latest
    needs: test          # nothing is built, let alone deployed, until tests pass
    permissions:
      contents: read #This is required for actions/checkout

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python version
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      # 🛠️ Local Build Section (Optional)
      # The following section in your workflow is designed to catch build issues early on the client side, before deployment. This can be helpful for debugging and validation. However, if this step significantly increases deployment time and early detection is not critical for your workflow, you may remove this section to streamline the deployment process.
      - name: Create and Start virtual environment and Install dependencies
        run: |
          python -m venv antenv
          source antenv/bin/activate
          pip install -r requirements.txt
                
      # By default, when you enable GitHub CI/CD integration through the Azure portal, the platform automatically sets the SCM_DO_BUILD_DURING_DEPLOYMENT application setting to true. This triggers the use of Oryx, a build engine that handles application compilation and dependency installation (e.g., pip install) directly on the platform during deployment. Hence, we exclude the antenv virtual environment directory from the deployment artifact to reduce the payload size. 
      - name: Upload artifact for deployment jobs
        uses: actions/upload-artifact@v4
        with:
          name: python-app
          path: |
            .
            !antenv/

      # 🚫 Opting Out of Oryx Build
      # If you prefer to disable the Oryx build process during deployment, follow these steps:
      # 1. Remove the SCM_DO_BUILD_DURING_DEPLOYMENT app setting from your Azure App Service Environment variables.
      # 2. Refer to sample workflows for alternative deployment strategies: https://github.com/Azure/actions-workflow-samples/tree/master/AppService
      

  deploy:
    runs-on: ubuntu-latest
    needs: build
    
    steps:
      - name: Download artifact from build job
        uses: actions/download-artifact@v4
        with:
          name: python-app
      
      - name: 'Deploy to Azure Web App'
        uses: azure/webapps-deploy@v3
        id: deploy-to-webapp
        with:
          app-name: 'theclinicue'
          slot-name: 'Production'
          publish-profile: ${{ secrets.AZUREAPPSERVICE_PUBLISHPROFILE_FCDAAD213B40457192DCB8633A1F6D00 }}

      - name: Smoke-test the live deployment
        run: |
          set -e
          URL="${HEALTH_URL:-https://theclinicue.com/api/health}"
          echo "Checking $URL"
          for attempt in 1 2 3 4 5 6; do
            body=$(curl -fsS --max-time 30 "$URL") && break || {
              echo "attempt $attempt failed, retrying in 20s"; sleep 20;
            }
          done
          echo "$body"
          echo "$body" | grep -q '"status":"ok"'
          echo "$body" | grep -q '"database":"ok"'
          echo "$body" | grep -q '"environment":"production"'
          echo "Live deployment healthy."
